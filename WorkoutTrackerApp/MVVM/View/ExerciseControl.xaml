<UserControl x:Class="WorkoutTrackerApp.MVVM.View.ExerciseControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WorkoutTrackerApp.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Horizontal">
        <CheckBox IsChecked="{Binding IsSelected}"/>
        <StackPanel>
            <StackPanel Orientation="Horizontal" >
                <TextBlock Text="Exercise Name: " Foreground="White"/>
                <TextBlock Text="{Binding ExerciseName}" Foreground="White" />
                <TextBlock Text=" Weight: " Foreground="White"/>
                <TextBlock Text="{Binding Weight}" Foreground="White"/>
                <TextBlock Text=" Sets: " Foreground="White"/>
                <TextBlock Text="{Binding Sets}" Foreground="White"/>
                <TextBlock Text=" Reps: " Foreground="White"/>
                <TextBlock Text="{Binding Reps}" Foreground="White"/>

            </StackPanel>
        </StackPanel>
    </StackPanel>
    
    
</UserControl>
